const COMPONENT_ENUM = {
    COMPONENT: 'component',
    ENTRY: 'entry'
}
const COMPONENT_EMPTY_ENUM = {
    ENTRY: COMPONENT_ENUM.ENTRY,
}
const COMPONENT_CRUD_ENUM = {
    QUERY: "query",
    DIALOG: "dialog",
    TABLE: "table",
    ENTRY: COMPONENT_ENUM.ENTRY,
}
const COMPONENT_MATRIX_ENUM = {
    ENTRY: COMPONENT_ENUM.ENTRY,
}
/* Software Gen Code Placeholder */

module.exports = {
    COMPONENT_ENUM,
    COMPONENT_EMPTY_ENUM,
    COMPONENT_CRUD_ENUM,
    COMPONENT_MATRIX_ENUM,
    /* Software Gen Code Export Placeholder */
}