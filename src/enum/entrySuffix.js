const { PAGE_TYPE_ENUM } = require("./pageType")

const ENTRY_SUFFIX_ENUM = {
    [PAGE_TYPE_ENUM.EMPTY]:'',
    [PAGE_TYPE_ENUM.CRUD]:'Manage',
    [PAGE_TYPE_ENUM.MATRIX]:'Matrix',
    /* Software Gen Code Placeholder */
}

module.exports = {
    ENTRY_SUFFIX_ENUM
}